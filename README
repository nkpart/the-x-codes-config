h1. The X Codes Xcode Config

h2. Install

* Clone the repo, stick it anywhere you like. It doesn't matter
* Run 'rake install'. This will install symlinks from the repository versions of files into the places that Xcode looks for things.

